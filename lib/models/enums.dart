
enum Environment { dev, sandbox, staging, production, production_sandbox }

enum Product { auth, transactions, balance, identity, income, guarantors, periodic_transactions, periodic_balance, direct_debit}
